<?php

if ($op==INCLUIR||$op==ALTERAR)
	{
		if ($nome=='')
			$msg = "O nome não pode ser branco";

		//if (ctype_digit($campoI)!=1)
		//	$msg = "O campo campoI deve ter apenas números";
	} // INCLUIR ou ALTERAR


// Validação conforme a operação
if ($op==INCLUIR)
	{

	} // Incluir

if ($op==ALTERAR)
	{

	} // Alterar

if ($op==EXCLUIR)
	{

	} // Excluir

?>
